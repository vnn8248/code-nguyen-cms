{
  "routes": [
    {
      "method": "GET",
      "path": "/portfolios",
      "handler": "portfolios.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/portfolios/count",
      "handler": "portfolios.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/portfolios/:id",
      "handler": "portfolios.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/portfolios",
      "handler": "portfolios.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/portfolios/:id",
      "handler": "portfolios.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/portfolios/:id",
      "handler": "portfolios.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
